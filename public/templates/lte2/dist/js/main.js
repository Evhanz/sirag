/**
 * Created by root on 14/12/16.
 */


/* se comento todo el codigo apara ser usado luego
//traemos quien a sido el que llamó a este evento
function alerta(ev) {
    var src = window.event ? window.event.srcElement : ev.target;
    alert( "Has hecho click en la capa con id \""+src.id+"\"" );
    console.log(src.max);
}



//para validar los input de tipo y por cada input qe o requiera
function validateInput(ev) {

    //sacamos al elemento que llama al vento
    var src = window.event ? window.event.srcElement : ev.target;

    var bandera = validateFormat(src.dataset.type,src.max);

    

}

//para validar el tipo de texto que se tiene que aceptar
// y el tamaño maximo
function validateFormat(type, len) {


    //solo se a definido por ahora 2 tipos para validaciones
    //si es numero o solo texto

    if(type=='numero'){

    }

}
 */
